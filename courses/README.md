# Canonical Courses

Canonical course knowledge lives here. Each active course is declared in `manifest.json` and has parallel `en/` and `fr/` directories.

Use `pnpm course:add` and `pnpm module:add` rather than creating course/module paths manually.
